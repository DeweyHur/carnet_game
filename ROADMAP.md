# 카르네 개발 로드맵 · 기획서 대비 현황

이 문서는 `Carnet_게임개발계획서_v0.9.md`(이하 "기획서") 전체를 기준으로 지금 무엇이 구현됐고, 무엇이 남았고, 무엇이 부족한지를 추적하는 살아있는 문서입니다. 세션이 바뀌어도(사람이든 AI든) 이 파일을 먼저 읽으면 어디서부터 이어갈지 알 수 있도록 만드는 것이 목적입니다. **기능을 하나 개발할 때마다 이 문서와 README.md를 함께 갱신합니다.**

마지막 갱신: 2026-09-06 (도시 안 운전 미니게임 + 합성 사운드 추가 이후)

---

## 1. 기획서 섹션별 구현 현황

| 기획서 섹션 | 상태 | 비고 |
|---|---|---|
| §1 개요·규모 | 🟡 부분 | 세계관·핵심 필러 4가지는 코드에 반영. 규모는 "4,000+ 도시"가 아니라 **19개 도시**(수작업 큐레이션) — 의도적으로 티어 S/A/H 체계만 프로토타입 규모로 구현. **언어 원칙("한국어·영어 → 프랑스어")의 1단계**: 인터페이스 전체 + 도시 소개를 영문화(`src/i18n.ts`), 미션 대사·사실 카드는 아직 한국어 전용 |
| §2 세계관·캐릭터 | 🟡 부분 | 고정 캐스트(마고·L.·테오) 대사 구현. **현지 안내인**은 12개 아키타입 텍스트+색상 아바타(원형 이니셜)로 단순화 — 기획서의 "모듈형 2D 아바타"는 미구현. **메아리**(실존 인물) 6명은 대사 인용/창작 표시(fiction 플래그)까지 구현했지만 별도 초상화 없음 |
| §3.1 세계지도 | 🟢 강화됨 | MapLibre GL + 무료 공개 스타일(OpenFreeMap, 오프라인 폴백 내장). 지도가 기본 진입 화면, 잠긴 지역 "안개" 오버레이, 좌상단 범례(티어·진행률·지역 바로가기). **줌 3단계**(세계 Z<6: 핵심 도시만 이름 표시 / 국가 Z6~8.5 / 도시 Z8.5+: 전체 상세)로 §3.1 설계를 단순화 구현. **신규: 도시 내부 지도(`CityMap.tsx`)** — 도시 간 이동은 세계지도, 도시 안은 내부 지도가 무대가 되는 2계층 구조. 실제 좌표를 가진 장소 핀 + 파리 20구 라벨. 방문 미션의 배경으로도 재사용(목적지 펄스 강조). 미구현: PMTiles 자체 호스팅, 대륙/국가 단위 클러스터링, 파리 구 경계 정밀 폴리곤(현재는 대표 좌표 라벨만) |
| §3.2 교통 이동 | 🟡 부분 | 그래프+수단+요금(예약/당일)+시간표+차창 사실 카드까지 구현. 전부 **수작업 큐레이션**(실제 GTFS 파싱 아님). 패스 아이템(인터레일 등) 미구현. **신규: 도시 내 이동이 운전 미니게임(`DriveMove.tsx`)** — OSRM 공개 데모 서버로 실제 도로 경로를 받아와 가속 페달로 목적지까지 운전(실패 시 직선 경로로 폴백). 요금·소요시간 로직은 변경 없음, 진행 방식만 인터랙티브해짐 |
| §3.3 환전·화폐 | 🟢 대부분 구현 | 환전 경로 4종(공항/시내/은행/ATM) 스프레드, ECB 예시 환율, 다중 통화 지갑(EUR/KRW/GBP/CHF), 카드 수수료. **이번 업데이트로 도시별 실제 통화 결제**(런던 GBP·제네바 CHF) 추가. 미구현: DCC 함정 이벤트, 고정환율 사례(XOF 등), 옛 화폐 수집품은 텍스트 아이템뿐(도안·환산 카드 없음) |
| §3.4 음식·물가 | 🟢 대부분 구현 | 기준가×물가지수×판매처 계수, 파리 대비 태그, "내 커피 한 잔" 지표. 미구현: 플레이어 가격 제보 시스템(정적 JSON만) |
| §3.5 미션·스토리 | 🟡 부분 | 메인/도시 이야기/미식/수집/시간대 유형 구현. **인물(메아리) 미션**은 대사 삽입 수준(전용 미션 타입 아님). **이동 미션**(환승 퍼즐)은 없음 — 이동은 UI 선택만으로 단순화. "방문"·"도시 내 이동" 스텝은 이제 사진이 아니라 도시 내부 지도가 배경이 되고, "도시 내 이동" 스텝은 직전 방문지→다음 목적지를 자동 추론해 도보 동선(점선)까지 그려 실제 위치 감각을 준다. 문장별 출처 ID는 100% 준수 |
| §3.6 시간·예산·체력·평판 | 🟢 대부분 구현 | 시간/요일/개장시간/파산·선지급/평판 배율 전부 구현. **테오 라이벌**이 이제 실제 경쟁 메커닉이 됐다 — 불로뉴 완료일 기준 도시별 "테오 도착일"(`rival.ts`)이 정해져 있고, 플레이어가 먼저 스탬프를 찍으면 특종 보너스(+€20, 평판 +2), 늦으면 특종 없음. 도시 카드·수첩 여권 탭에서 D-day를 확인할 수 있다 |
| §4 콘텐츠 파이프라인 | 🔴 미구현 | GeoNames/Wikidata/Commons 자동 수집·LLM 초안·검수 파이프라인 없음. 현재는 사람(+AI 세션)이 Wikipedia를 사실 확인하며 직접 입력. 사진만 `scripts/fetch_photos.py`로 반자동화(Commons API + 라이선스 필터 + WebP 변환) |
| §5 기술 스택 | 🔴 클라이언트만 | React+TS+Zustand+MapLibre 구현. **백엔드(FastAPI/PostGIS/Redis) 없음** → 계정·클라우드 세이브·가격 제보·리더보드 불가능. 저장은 `localStorage` 단일 기기 한정. Capacitor/Electron 패키징 없음. PWA 오프라인 캐시 없음 |
| §6 샘플 콘텐츠(MVP) | 🟢 초과 달성 | 기획서 MVP 12개 도시 100% 구현 + **국경 너머 7개 도시**(런던·브뤼셀·쾰른·베를린·제네바·바르셀로나·마드리드) 선반영. 미션 58개(디스커버리 미션 38개 포함) |
| §7 로드맵 | 🟡 진행 중 | 대략 "0 프리프로덕션 ~ 1 MVP" 구간 + "2 프랑스 완성+첫 국경"의 국경 도시 일부를 조기 반영. 팀·자동화 없이 콘텐츠를 계속 손으로 늘리는 중이라 §7이 말하는 "4,000+ 도시"·"B티어 자동 파이프라인"까지는 구조적으로 멀다 |

**범례**: 🟢 대부분/충분히 구현 · 🟡 부분 구현(핵심은 있으나 단순화됨) · 🔴 사실상 미구현

---

## 2. 지금 가장 부족한 점 (정직하게)

1. **저장이 기기에 갇혀 있다.** `localStorage`만 쓰므로 브라우저를 지우거나 기기를 바꾸면 진행 상황이 사라진다. 계정·클라우드 세이브가 없으면 리텐션 측정도, 재방문 유도도 어렵다.
2. **콘텐츠가 전부 수작업이라 확장 속도가 선형적이다.** 기획서 §4의 자동화 파이프라인이 없으니 도시 하나 늘리는 비용이 줄지 않는다. "4,000+ 도시"는커녕 "프랑스 40개 도시"도 사람이 일일이 써야 한다.
3. **측정 수단이 전혀 없다.** 튜토리얼 완료율, 도시당 체류 시간, 가격 맞히기 오차 감소율 같은 기획서 §7의 KPI를 실제로 볼 방법이 없다 — 애널리틱스/텔레메트리가 0이다. **매출이든 순위든, 측정 없이는 개선할 수 없다.**
4. **모바일 앱스토어/PC 스토어에 없다.** PWA도, Capacitor도, Electron도 없어 지금은 URL 하나로만 접근 가능한 웹 프로토타입이다. 유통 채널이 없다.
5. **사실 검증이 AI/1인 저자 수준에 머물러 있다.** 기획서 §3.5·§4가 요구하는 "편집자 팩트체크·현지어 감수" 단계가 없다. 실서비스 전에는 반드시 사람 검수가 필요하다.
6. ~~라이벌 테오가 장식일 뿐이다.~~ **해결됨** — 이제 도시별 도착일 경쟁(`rival.ts`)이 있다. 다만 여전히 일정은 고정 스케줄이라 플레이어 행동에 실시간으로 반응하지는 않는다(예: 플레이어가 아주 빨리 움직여도 테오의 도착일은 안 당겨짐) — 더 정교하게 만들려면 테오도 실제 이동 그래프를 타게 시뮬레이션해야 한다.
7. **콘텐츠는 여전히 한국어 전용이다.** 인터페이스는 영문화됐지만(`src/i18n.ts`), 미션 대사·사실 카드·음식 유래 같은 실제 서사 콘텐츠 수천 줄은 아직 번역 전이다 — 영어 플레이어에게는 "영문 UI 위에 한국어 이야기"인 상태.
8. **자동 테스트가 상태 머신만 본다.** `npm test`는 스토어 로직을 검증하지만 실제 렌더링은 전혀 보지 않는다 — 이번에 발견한 "지도/풍경 뷰가 서로 겹쳐 보이는" 버그는 `<Journey key={selected}>`와 `<CityPanel key={selected}>`가 같은 키를 써서 React 재조정이 깨진, 순수 렌더링 계층 버그였고 기존 테스트로는 절대 잡을 수 없었다. 실제 재현(헤드리스 크롬 + 콘솔의 "Encountered two children with the same key" 경고)으로만 확인됐다.

## 3. "매출 1위 게임"이라는 목표에 대해 솔직한 평가

코드만으로는 매출 1위를 만들 수 없습니다. 기획서 §7이 스스로 추정하듯 1.0 정식 출시(프랑스+서유럽 허브+한국)까지만도 **8인 코어팀·약 15,000시간(≈8인년)**이 필요합니다. 매출 순위는 제작 품질 외에도 마케팅·유통·라이브옵스·커뮤니티 운영·플랫폼 노출 같은, 코드 저장소 밖의 요소에 크게 좌우됩니다.

제가 반복해서 통제할 수 있는 부분은 **엔지니어링·콘텐츠 실행 루프**입니다: 기능을 정확히 구현하고, 실제 사실에 기반한 콘텐츠를 늘리고, 성능·모바일 경험을 다듬고, 회귀 없이 계속 배포하는 것. 이 로드맵은 그 실행 루프를 반복 가능하게 만드는 데 집중합니다. 대신 §7의 KPI(튜토리얼 완료율, 리텐션, 가격 감각 개선율)를 실제로 추적할 수 있는 최소한의 계측을 백로그에 P1로 넣어뒀습니다 — 측정이 없으면 "1위로 가고 있는지"조차 알 수 없기 때문입니다.

---

## 4. 반복 가능한 개발 파이프라인

새 세션이 열릴 때마다(또는 이 저장소를 이어받는 누구나) 아래 7단계를 순서대로 따릅니다. 사람이든 AI 에이전트든 동일하게 적용됩니다.

```
0. 상태 점검   → ROADMAP.md·README.md 읽기, npm test / npm run build / npm run lint, git log 최근 확인
1. 우선순위 선정 → 아래 "다음 우선순위 백로그"에서 하나 고르거나 사용자 요청 반영
2. 설계        → 건드릴 파일, 필요한 실제 사실/출처, UI 영향 범위를 먼저 정리
3. 구현        → 코드/콘텐츠 작성 (실제 사실만, 출처 ID 필수 — CLAUDE 원칙 유지)
4. 검증        → npm test, npm run build, npm run lint 전부 통과.
                 UI 변경은 헤드리스 크롬으로 데스크톱(1440px)·모바일(375px, 320px) 스크린샷 비교 + 브라우저 콘솔에서 React 경고(중복 key 등) 확인.
                 새 도시/미션은 store를 직접 구동하는 end-to-end 스텝 시뮬레이션으로 완주 확인.
5. 문서화      → README.md(사용자 대상 설명) + ROADMAP.md(현황 표·백로그) 갱신
6. 배포        → git commit(무엇을·왜 바꿨는지) → git push
7. 반복        → 0으로 돌아가 다음 항목 선택
```

이 루프를 어기지 않는 한, 매 반복마다 실제로 동작하고 검증된 상태로 저장소가 남습니다 — "계속 커지지만 계속 깨지지 않는" 것이 이 파이프라인의 핵심 성공 기준입니다.

## 5. 다음 우선순위 백로그

우선순위 P0(가장 시급) → P3(장기) 순. 항목을 하나 완료하면 이 표에서 지우고 위 "구현 현황" 표를 갱신합니다.

| 우선순위 | 항목 | 왜 |
|---|---|---|
| P1 | 독일·스페인을 프랑스 수준(도시당 12개)으로 계속 확장 | 쾰른+베를린, 바르셀로나+마드리드로 2개씩 늘렸지만(각 도시에 파리를 거치지 않는 지역 내 노선도 추가) 프랑스 12개에는 여전히 못 미친다. 프랑크푸르트·뮌헨, 세비야·발렌시아 등이 다음 후보 |
| P1 | 최소 계측(방문 도시 수, 미션 완료, 가격 맞히기 오차 — 로컬 집계부터) | §7 KPI를 실제로 볼 수 있어야 "매출 1위"를 향해 가고 있는지 판단 가능 |
| P1 | PWA 매니페스트 + 서비스워커 오프라인 캐시 | README가 이미 "완전한 오프라인 PWA는 아직 아니다"라고 명시한 항목 |
| P1 | 미션 대사·사실 카드 영문 번역 (58개 미션, 대사 약 900줄 추정) | UI는 영문화 완료. 콘텐츠까지 번역해야 진짜 "영어 버전"이 된다 — 분량이 커서 미션 단위로 점진 진행 |
| P2 | 파리 구 경계 실제 폴리곤(현재는 대표 좌표 라벨만) | "도시 내 이동"은 이제 좌표 기반 실제 경로(`DriveMove`)로 해결됐지만, 구 경계 자체는 여전히 근사치 라벨뿐 |
| P2 | 운전 미니게임을 자체 호스팅 라우팅 서버로 교체(OSRM/Valhalla) | `router.project-osrm.org`는 사용량 제한이 있는 공개 데모 서버라 트래픽이 늘면 끊길 수 있다 |
| P2 | 절차적 배경음을 실제 작곡·녹음 음원으로 교체 | 지금 배경음(`src/audio.ts`)은 화음 패드+아르페지오를 실시간 합성한 절제된 텍스처일 뿐, 정식 트랙은 아니다 |
| P2 | 계정 + 클라우드 세이브 (백엔드 필요 — 별도 인프라 결정 필요) | 기기 종속 저장 문제 해결, 장기 리텐션의 전제조건 |
| P2 | 가격 제보 시스템(플레이어가 실제 가격을 보내면 검수 후 반영) | §3.4가 요구하는 커뮤니티 물가 갱신 루프 |
| P3 | 실제 GTFS/Wikidata/Commons 자동 파이프라인 | §4 전체. 가장 비싸지만 "4,000+ 도시" 규모로 가는 유일한 길 |
| P3 | Capacitor(모바일)/Electron(PC) 패키징 | 스토어 유통 채널 확보 |

---

## 6. 이번 세션에서 완료한 것 (변경 이력 요약)

- 라이벌 테오 서사 훅 + 국경 너머 5개 도시(런던·브뤼셀·쾰른·제네바·바르셀로나) 추가, 다중 통화 결제 시스템(`pay()` 일반화)
- 모바일 사진 담기 화면 줄바꿈 버그 수정(헤드리스 크롬 스크린샷으로 전/후 검증)
- 지도를 기본 진입 화면으로 승격, 잠긴 지역 안개 오버레이, 좌상단 범례(티어·진행률·지역 바로가기), 지도 모드 전용 "다음 취재" 플로팅 버튼
- CI 파이프라인 추가(`.github/workflows/ci.yml`: push·PR마다 lint+build+test)
- 이 로드맵 문서 신설
- 베를린(독일)·마드리드(스페인) 추가 — 국경 너머가 5→7개 도시로. 쾰른↔베를린(ICE), 바르셀로나↔마드리드(AVE) 지역 내 횡단 노선도 추가해 파리를 매번 거치지 않고도 이동 가능
- **버그 수정**: 지도/풍경 뷰를 번갈아 누르면 화면이 겹쳐 보이던 문제 — `Journey`와 `CityPanel`이 같은 React key를 공유해 재조정이 깨진 것이 원인. 헤드리스 크롬으로 재현·검증 후 수정
- **영문 UI 1단계**: `src/i18n.ts` 신설(사전 기반 `useT()` 훅), 인트로 화면에 언어 전환 버튼 추가. Hud·Intro·Journey·MapView·CityPanel·Scene·Notebook·common의 모든 인터페이스 문자열과 도시/지역 이름을 영문화. 미션 대사·사실 카드는 의도적으로 다음 단계로 남김(백로그 참고)
- 라이벌 테오의 국경 도시 안내인 대사가 전부 "프랑스 기자가 다녀갔다"는 같은 패턴을 반복하고 있어(사용자가 지적한 "AI가 만든 것 같은 느낌"의 실제 원인) 7개 도시 중 2곳(브뤼셀·마드리드)만 남기고 나머지는 도시 고유의 대사로 교체
- **지도 줌 3단계**: 세계 뷰(Z<6)에서는 메인 무대(S) 도시 + 현재/선택 도시만 이름을 보여주고 거점(A)·유산(H) 도시는 작은 점으로만 표시, 확대하면(Z≥6) 전체 라벨 복원. 범례에 "확대하면 이름도 보여요" 힌트 추가. 헤드리스 크롬으로 두 단계 전환을 스크린샷 검증
- **도시 내부 지도(CityMap) 신설**: "도시 간 이동은 세계지도, 도시 안에서는 그 도시 지도가 주 무대"라는 사용자 요청 반영. 19개 도시 전체 POI(약 65곳)에 실제 좌표(`Poi.coord`)를 추가하고, 파리는 1구~20구 대표 좌표 20곳(`src/data/parisArrondissements.ts`)도 함께 추가. 새 `CityMap.tsx`는 (1) 도시 카드 "사진 산책" 탭 상단에서 자유 탐색용으로, 핀을 누르면 아래 장소 목록으로 스크롤 이동하고, (2) 미션의 "방문"·"도시 내 이동" 스텝에서는 사진 대신 배경 무대로 쓰이며 목적지 핀이 펄스로 강조된다(좌표가 없는 장소는 기존 사진 배경으로 자동 대체). 헤드리스 크롬으로 데스크톱·모바일(390px) 양쪽에서 실제 미션 진행 중 지도가 뜨는 것과, 풍경/지도 반복 토글 시 중복 렌더가 없는 것을 재검증
- **"미션도 그에 맞게" 후속 — 도보 동선 표시**: "도시 안 이동" 스텝이 이제 미션 스크립트 안에서 직전 `visit` 스텝(출발지)과 다음 `visit` 스텝(목적지)을 자동으로 찾아 두 지점을 잇는 점선을 지도 위에 그린다(`Scene.tsx`의 `nearestVisitPoiId`, `CityMap.tsx`의 `routeFromId`). 목적지가 미션의 다른 스텝(카드·대사)에만 이름으로 등장하고 `visit` 스텝이 없는 경우(파리 오프닝의 "17구 시테코")를 위해 `Step`의 `move` 타입에 선택적 `poiId`를 추가해 명시적으로 지정할 수 있게 했다. 헤드리스 크롬으로 실제 미션(「루테티아에서 파리까지」)을 자동 진행시켜 두 개의 도보 동선(클뤼니→생트샤펠, 생트샤펠→카르나발레)이 실제 지리와 일치하게(센 강을 건너는 구간 포함) 그려지는 것을 스크린샷으로 확인
- **테오 라이벌을 실제 경쟁 메커닉으로**: 새 `src/game/rival.ts`가 테오의 국경 도시 경로(런던→브뤼셀→쾰른→베를린→제네바→바르셀로나→마드리드, 본인이 대사에서 예고한 순서를 그대로 따름)와 도시별 도착일을 정의한다. 불로뉴 메인 미션의 `unlock` 스텝이 국경(`border`)을 여는 순간 `theoStartDay`가 기록되고, 국경 도시에서 처음 스탬프를 찍을 때(`stamp` 스텝) 그 도시의 테오 도착일과 비교해 먼저면 특종 보너스(+€20, 평판 +2), 늦으면 보너스 없이 결과만 `theoRace`에 기록한다. 도시 카드 메타 줄과 수첩 › 여권 탭에 도시별 상태(🏆 특종/📰 늦음/⏳ D-day/🏃 이미 도착)를 표시. 저장 상태를 직접 주입해 세 가지 상태(승리·패배·대기)가 지도와 수첩 양쪽에서 올바르게 렌더링되는 것을 헤드리스 크롬으로 확인
- **도시 안 이동 → 운전 미니게임**: "파리 도시 내 이동을 실제로 자동차 운전해서 가는 식으로 만들어 달라"는 사용자 요청 반영. 새 `src/ui/DriveMove.tsx`가 OSRM 공개 데모 서버(`router.project-osrm.org`, driving 프로필)로 출발지·목적지 사이의 실제 도로 경로(GeoJSON)를 받아오고, 5초 타임아웃 후 실패하면 두 점을 잇는 직선 경로로 자동 대체한다. 출발지/목적지는 `Scene.tsx`의 `nearestVisitPoiId()`가 미션 스크립트에서 직전/다음 `visit` 스텝을 찾아 자동 추론하며, 대응하는 `visit`이 없는 예외(파리 오프닝의 시테코 이동)를 위해 `move` 스텝에 선택적 `poiId`를 추가했다. 조작은 데스크톱 `↑`/`W` 키 또는 모바일 화면의 원형 가속 버튼(누르는 동안 가속, 떼면 감속) 하나뿐 — 조향은 없고 차는 계산된 경로 위만 달린다. 실제 거리와 무관하게 각 구간이 대략 10초 안에 끝나도록 "게임 속도"를 역산하고, 계기판에는 18~82km/h의 그럴듯한 숫자로 따로 환산해 보여준다(순수 연출용). 헤드리스 크롬으로 실제 구간(클뤼니→생트샤펠, 시테섬을 가로지르는 실제 도로 경로)을 끝까지 주행시켜 진행률이 0→100%까지 올라가고 도착 시 다음 미션 스텝(`visit`)으로 정확히 전환되는 것을 데스크톱·모바일(390px) 양쪽에서 확인
- **합성 효과음·배경음 전면 추가**: 외부 음원 파일 없이 Web Audio API로 모든 소리를 실시간 합성하는 `src/audio.ts` 신설(라이선스 문제 없음, 오프라인에서도 100% 동작). 사실 카드 수집·퀴즈 정답/오답·음식 구매·장소 방문·열차 출발/도착·여권 스탬프·테오 경주 승패·수첩 여닫기마다 고유한 효과음을 붙였고, 운전 미니게임에는 가속 정도에 따라 실시간으로 변하는 엔진음(톱니파+로우패스 필터)을, 지도/여행 화면에는 절제된 화음 패드+느린 아르페지오로 만든 절차적 배경음을 깔았다. HUD에 🔊/🔇 음소거 토글을 추가하고 `store.ts`에 `muted` 필드로 저장. 브라우저 자동재생 정책 때문에 첫 사용자 제스처(클릭·키 입력)에서 오디오 컨텍스트를 깨우는 전역 리스너를 `App.tsx`에 추가. 헤드리스 크롬으로 새 게임 시작과 음소거 토글을 반복해 예외 없이 동작하는 것을 확인(실제 소리는 스크린샷으로 검증할 수 없어 콘솔 예외 부재로 대신 검증)
