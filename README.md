# 카르네(Carnet): 세계를 걷는 기록 — 지도 중심 업데이트

`Carnet_게임개발계획서_v0.9.md`의 6장(샘플 콘텐츠·MVP)을 플레이 가능한 웹 프로토타입으로 옮긴 것입니다.
일드프랑스 6개 도시 + 프랑스 북부·중부 6개 도시, 그리고 국경 너머 7개 도시(런던·브뤼셀·쾰른·베를린·제네바·바르셀로나·마드리드)까지 총 19개 도시. 이야기 13개 + 국경 도시 이야기 7개 + 도시별 사진 취재·미식 체험 38개를 더해 총 58개 미션을 제공합니다.

> **지금 어디까지 구현됐고 뭐가 남았는지**는 [`ROADMAP.md`](./ROADMAP.md)에서 기획서 섹션별로 추적합니다. 이 저장소를 다시 열 때는 그 문서를 먼저 읽고, 완료한 항목은 지우고, 새로 한 일을 적어 넣습니다.

**지도가 이 게임의 중심입니다.** 시작하면 바로 세계지도가 열립니다 — 사진 산책(풍경 뷰)은 지도 안에서 도시를 고른 다음에 보는 보조 화면이고, 여행의 본체는 지도 위에서 핀을 고르고 노선을 잇는 것입니다. 지도에는 티어(S/A/H)·잠긴 지역의 "안개"·진행률·지역 바로가기를 보여주는 범례가 떠 있고, 잠긴 지역은 각 도시 주변에 반투명한 안개로 표시되다가 해금되면 걷힙니다(기획서 §3.1). 진행 중인 미션이 있으면 지도 위에 떠 있는 "다음 취재" 카드로 바로 이어갈 수 있습니다. 줌아웃해서 유럽 전체를 보면 번잡함을 줄이려고 메인 무대(S) 도시 이름만 남고 거점·유산 도시는 점으로만 표시되며, 확대하면 다시 전체 이름이 나타납니다.

큰 현장 사진과 도시 엽서 목록에서 여행을 시작하고, 지도 보기로 동선을 확인합니다. 사진 산책에서 장소를 방문하거나 현지 음식을 맛보면 사진과 방문 기록이 앨범에 쌓입니다. 사진은 확대할 수 있으며 촬영자·라이선스·원본 링크를 함께 표시합니다.

가격 맞추기는 직접 입력 없이 네 가지 가격표 중 하나를 고르는 방식입니다. 영수증에서 실제 가격, 자국 통화 환산액, 예상가와의 차이를 확인합니다. 답안·구매·기사 결과는 저장되므로 잠시 접거나 새로고침해도 결제와 보상이 중복되지 않습니다. 기존 `carnet-save-v1` 저장을 유지합니다.

**라이벌 테오와 국경 너머.** 불로뉴비양쿠르의 메인 미션 「지구의 기록」을 마치면, 라이벌 작가 테오가 끼어들어 런던·브뤼셀·쾰른·베를린·제네바·바르셀로나·마드리드로 먼저 떠났다고 선언합니다. 이 순간 프랑스 북부·중부의 소도시들과 함께 일곱 국경 도시가 한꺼번에 지도에서 열립니다 — 아미앵·랭스 같은 소도시부터 걸을지, 테오보다 먼저 국경을 넘을지는 플레이어가 정합니다. 런던(파운드)과 제네바(스위스 프랑)는 진짜 환전이 필요한 국경이고, 브뤼셀·쾰른·베를린·마드리드는 국경을 넘어도 유로를 그대로 쓰는 "화폐가 같은 국경"입니다(기획서 §3.3). 쾰른↔베를린, 바르셀로나↔마드리드처럼 파리를 거치지 않고 국경 지역 안에서 바로 이동하는 노선도 있습니다.

**한국어·English.** 인트로 화면과 게임 안 어디서든 언어를 바꿀 수 있습니다(`src/i18n.ts`). 인터페이스(버튼·탭·HUD·안내 문구)와 도시 소개는 완전히 영문화되어 있습니다. 미션 대사·사실 카드·음식 유래 같은 서사 콘텐츠는 아직 한국어 전용입니다 — 기획서 §1이 말하는 "UI 먼저, 콘텐츠는 점진적으로" 원칙을 그대로 따른 것이며, 사전에 없는 문자열은 원문(한국어)으로 자연스럽게 대체됩니다.

## 실행

```bash
nvm use            # Node 22.12 이상 (.nvmrc: 22)
npm install
npm run dev        # http://localhost:5173
npm run build      # dist/ 정적 번들 (PWA/Capacitor/Electron의 입력)
npm test           # 데이터·사진·선택지·저장 호환·결제/보상 검증
npm run lint
```

## 실제 사진

`public/photos/`의 WebP 사진과 작은 썸네일을 배포에 포함합니다. 플레이 중 Wikimedia에 사진 요청을 보내지 않습니다. 다운로드 재현은 Python 3 + Pillow 환경에서 `python scripts/fetch_photos.py`로 가능합니다. 기존 파일은 건너뛰며, 교체는 `--refresh 사진ID`를 사용합니다.

촬영자·원본 파일·라이선스·변경 내역은 `src/data/photoManifest.json`에 기록됩니다. 사진의 개별 라이선스는 코드와 별개로 적용됩니다. 지도 타일과 웹 글꼴에는 인터넷 연결이 필요하며 완전한 오프라인 PWA는 아직 아닙니다.

지도 타일은 OpenFreeMap(무료 공개 스타일)을 쓰고, 네트워크가 막히면 내장된 프랑스 윤곽 폴백 스타일로 자동 전환합니다.
실서비스에서는 기획서 §3.1대로 PMTiles 자체 호스팅으로 교체하면 됩니다(`src/ui/MapView.tsx`의 `STYLE_PRIMARY`).

## 구조

```
src/
  game/
    types.ts      도시·POI·음식·이동 엣지·사실 카드·미션 스텝 타입 (기획서 §4 스키마 축약). City.currency로 도시별 실제 통화(GBP/CHF 등) 지정
    economy.ts    환율(ECB 예시)·환전 경로 5종 스프레드·가격 산정(기준가×물가지수×판매처 계수)·기사 등급·통화 변환(convert/cityCurrency)
    store.ts      Zustand 게임 상태: 시간/요일·지갑(다중 통화+카드 수수료)·체력·평판·부채·미션 진행·저장(localStorage). pay()가 도시 통화로 결제, 부족분은 카드→편집장 선지급 순으로 처리
  data/
    cities.ts     19개 도시 노드(프랑스 12 + 국경 너머 7: 런던·브뤼셀·쾰른·베를린·제네바·바르셀로나·마드리드) + 이동 그래프(§3.2 MVP 이동표: 수단·소요·요금[예약/당일]·편수·첫차/막차·차창 사실). 파리 허브 노선 외에 쾰른↔베를린·바르셀로나↔마드리드 같은 지역 내 횡단 노선도 있음
    cards.ts      사실 카드 80여 장 — 문장마다 출처 ID
    sources.ts    출처 목록(Wikipedia·Wikidata·기관 공식) — 수첩에서 '출처 보기'
    missions.ts   미션 스크립트(오프닝 튜토리얼, 메인 「지구의 기록」, 아미앵 풀 미션 「80일의 첫 페이지」, 프랑스 도시 이야기 11개, 국경 도시 이야기 7개)
    fallbackMap.ts 오프라인 폴백 지도
  ui/
    MapView.tsx   MapLibre 지도(게임의 기본 진입 화면): 도시 핀(티어·잠김·현재·스탬프), 잠긴 지역 안개 오버레이, 좌상단 범례(티어·진행률·지역 바로가기), 노선, 이동 애니메이션
    CityPanel.tsx 도시 카드 4탭(미션·장소·이동·음식) — 기차표 UI, 현지+자국 통화 병기, 파리 대비 태그
    Scene.tsx     비주얼노벨식 미션 러너: 대사/사실 카드/퀴즈/포토 매칭/순서 퍼즐/방문(개장 요일 검사)/구매(가격 맞히기)/환전/기사 조립/편지/해금/스탬프
    Notebook.tsx  수첩·지갑(환율 보드·환전)·여권(스탬프·수집품)·기사·편지
    Hud.tsx, Intro.tsx, common.tsx
  i18n.ts       한국어→영어 UI 사전 + useT() 훅. lang은 store.ts에 저장(게임 리셋과 무관하게 유지)
```

## 다중 통화 시스템

12개 프랑스 도시는 모두 유로(EUR)를 쓰지만, 국경 너머 7개 도시는 실제 통화가 다릅니다: 런던은 파운드(GBP), 제네바는 스위스 프랑(CHF), 브뤼셀·쾰른·베를린·바르셀로나·마드리드는 프랑스와 같은 유로입니다. `City.currency` 필드로 지정하며, 입장료(`feeEur`)·음식 기준가(`baseEur`)·숙박비(`hostelEur`)·구역 1회권(`transitFareEur`)은 모두 **그 도시의 실제 통화 기준 금액**으로 입력합니다(필드 이름은 하위 호환을 위해 그대로 뒀습니다). 도시 간 열차·유로스타 요금(`Edge.fareEur`)만은 실제 국제선 관행대로 항상 유로로 판매되는 것으로 통일했습니다. 지갑 부족 시 카드 결제(자국 통화, 수수료 1.5%) → 편집장 선지급(€300 상당, 이자 10%) 순으로 처리되며, 어느 통화로 결제하든 동일한 로직(`store.ts`의 `pay()`)을 씁니다.

## 미션 스텝 추가하기

`src/data/missions.ts`의 `steps` 배열에 스텝을 넣으면 됩니다. 스텝 타입은 `src/game/types.ts`의 `Step`:

| 타입 | 설명 |
|---|---|
| `say` | 대사. `who`: margot / guide / L / theo / echo / narrator. echo는 `fiction` 플래그로 창작/인용 표시 |
| `card` | 사실 카드 수집 (`cardId`는 `cards.ts`에 있어야 함) |
| `quiz` | 3지선다. 정답이든 오답이든 카드는 주고, 오답은 기사 정확도에 반영 |
| `photo` | L.의 사진 ↔ 현재 장소 매칭 |
| `order` | 순서 퍼즐. `items`는 정답 순서(UI에서 섞음) |
| `visit` | 장소 방문: 입장료·소요 시간·휴관 요일(실제 데이터) 검사, 22시 이후 불가 |
| `buy` | 음식 구매. `guess: true`면 가격 맞히기(오차가 KPI로 기록됨) |
| `exchange` | 환전(환전 경로 선택). `from`/`to` 통화를 지정 — 런던·제네바처럼 실제 환전이 필요한 도시에서 쓴다 |
| `move` | 도시 내 구역 이동(1회권. 도시별 `transitFareEur`, 없으면 €2.50 상당) |
| `article` | 사실 카드로 기사 조립 → 등급(C/B/A/S) → 원고료 |
| `letter` / `unlock` / `collect` / `stamp` | L.의 편지 / 지역 해금 / 수집품 / 여권 스탬프 |

## 기획서 대비 현황 · 다음 단계

짧게 요약하면: 클라이언트 쪽 핵심 루프(지도·이동·환전·미식·미션·기사)는 기획서 §3 대부분을 구현했지만, §4(자동 콘텐츠 파이프라인)와 §5(백엔드·계정·모바일 패키징)는 사실상 없습니다 — 지금은 손으로 계속 도시를 늘려나가는 단계입니다. 섹션별 구현 현황표, 부족한 점, 그리고 다음에 할 일의 우선순위 백로그는 [`ROADMAP.md`](./ROADMAP.md)에 정리되어 있으며 개발이 진행될 때마다 갱신됩니다.
